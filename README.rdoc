= SSL Install Tool
== What
SSL is gem that automates creation, validation and installation of ssl certificate on the server. It detects the
web sites on the server and creates and sends the certificate requests to an globally trusted certificate authority.
Optionally, it does the validation and installation of the signed certificate.
== Installing
  gem install ssl
